(element authmanage-section
  (with-description "La sezione di gestione autorizzazioni. Qui l'utente può  visualizzare tutte le associazioni utente/ruolo, associare un utente a un determinato ruolo (e naturalmente revocarne l'associazione)"
    (label (const "TBD"))))
